CREATE TABLE Pokedex(
    name TEXT NOT NULL,
    url TEXT NOT NULL
);

insertPokemon:
INSERT INTO Pokedex(name,url) VALUES (?,?);

deleteAllPokemon:
DELETE FROM Pokedex;

selectAllPokemon:
SELECT Pokedex.* FROM Pokedex;